{
  "apps": [
    {
      "appId": "d53cd738",
      "root": "./",
      "webDir": "dist",
      "dependencyInstallCommand": "npm install",
      "buildCommand": "npm run build && (npx cap add ios || true) && npx cap sync ios"
    }
  ]
}
